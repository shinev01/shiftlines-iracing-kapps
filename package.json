{
  "name": "iracing-shiftlines-kapps",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check:data": "node scripts/sync-car-data.mjs --check",
    "sync:data": "node scripts/sync-car-data.mjs --write"
  }
}
